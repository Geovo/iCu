#include <stdio.h>


// /|\PROGRESS: UNDONE

//char month_of_year[] (int)

// here goes the main function

int main(int argc, char *argv[])
{
    // awesome code goes here:
    char *stuff[] = {"hello", "my", "name", "is", "Ckreator"};
    char *p = *stuff;
    //for (int i; i < 5; i++, p++)
        printf("%s ", p);

    return 0;
}
